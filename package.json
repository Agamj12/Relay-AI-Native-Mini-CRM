{
  "name": "relay-mini-crm",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "node --env-file=.env --no-warnings server/index.js",
    "channel": "node --env-file=.env --no-warnings channel/index.js",
    "dev": "concurrently -n crm,channel,web -c green,yellow,cyan \"npm run server\" \"npm run channel\" \"npm --prefix web run dev\"",
    "build": "npm --prefix web install && npm --prefix web run build",
    "start": "concurrently -n crm,channel \"npm run server\" \"npm run channel\"",
    "e2e": "./e2e.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "concurrently": "^10.0.3",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "mongodb": "^6.21.0"
  },
  "engines": {
    "node": ">=22.5"
  }
}
